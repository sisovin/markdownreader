{
  "name": "markdown-reader-project",
  "version": "1.0.0",
  "private": true,
  "description": "Native PHP markdown reader with login, sign up, and admin dashboard.",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./resources/css/app.css -o ./public/assets/css/app.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./resources/css/app.css -o ./public/assets/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.2.1",
    "tailwindcss": "4.2.1"
  }
}